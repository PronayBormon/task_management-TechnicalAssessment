name: Deploy project to server
on:
  push:
    branches:
      - dev
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.1.0
        with:
          fetch-depth: 2
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ftp.impronay.xyz
          username: pronay@task.impronay.xyz
          password: "C@+HS?Av1X9{R$f{"
